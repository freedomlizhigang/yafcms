技术细节规类：

	密码处理原则？ 增加密码加密因子存储到数据库中。当进行用户相关操作时，要验证密码与加密因子结合后的结果。即：md5(md5(iptpwd)).encrypt === password

	验证码使用Checkcode库创建(取自PHPCMS)


已经完成的：

	验证码完成
	
	更改后台框架，回归原始方式iframe；现有方式(将菜单存到session中)对于数据库查询过多（使用session仍不能完美解决该问题，且刷新是个问题）。
	
	session做方式由files改为数据库(mysql)

	跳转的时候会出现跳转url错误的问题location.href = href 这个的问题，加上base标签，让所有的url都基于域名地址

	完善page分页功能，改?page=1为伪静态形式/page/1

	日志功能完善

	缓存类（只做了文件形式的缓存）

	用户及组管理，权限管理

	数据库模型新增加了批量添加数据方法insert_all

	用户资料修改，密码修改 完成

	富文本编辑器、图片上传

	富文本编辑器及图片上传功能完善

	栏目管理功能

	更新缓存方式改为类库library

	增加默认session路径及时间配置项

	改进上传类，增加路径识别，如果没有则新建（没有默认值，要在实例化类时定义）

	文章管理，增加时间选择

	增加：namespace(命名空间)支持

	友情链接完成

	添加程序分析器profiler/Benchmark(计算时间及内存)

	添加错误处理功能Error

	改写PDO方式连接数据库

下一步：

	关联、推荐、类别

	前台模板（angularJS）

	前台用户功能

	微信api